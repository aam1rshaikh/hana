{
  "buildCommand": "npm install --legacy-peer-deps && CI=false react-scripts build",
  "outputDirectory": "build",
  "env": {
    "PUBLIC_URL": "/"
  },
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}